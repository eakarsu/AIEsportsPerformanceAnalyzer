'use strict';
const STATES = Object.freeze({ ingested: ['metrics_ready', 'withdrawn'], metrics_ready: ['coach_review'], coach_review: ['approved', 'rejected'], approved: ['active'], active: ['completed', 'withdrawn'], rejected: [], withdrawn: [], completed: [] });
function normalizeMetric(input) { if (!input || !input.definitionVersion || !input.gamePatch || !input.sourceChecksum) throw new Error('definitionVersion, gamePatch and sourceChecksum are required'); const numerator = Number(input.numerator); const denominator = Number(input.denominator); if (!Number.isFinite(numerator) || !Number.isFinite(denominator) || denominator <= 0) throw new Error('valid numerator and positive denominator required'); return { value: numerator / denominator, definitionVersion: input.definitionVersion, gamePatch: input.gamePatch, sourceChecksum: input.sourceChecksum }; }
function assertTransition(from, to, context = {}) { if (!(STATES[from] || []).includes(to)) throw new Error(`invalid transition ${from} -> ${to}`); if (to !== 'withdrawn' && !context.consentActive) throw new Error('active player consent required'); if (to === 'approved' && context.role !== 'coach') throw new Error('coach review required'); return true; }
module.exports = { STATES, normalizeMetric, assertTransition };
