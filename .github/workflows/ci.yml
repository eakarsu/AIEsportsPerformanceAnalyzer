name: CI
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env: { POSTGRES_USER: postgres, POSTGRES_PASSWORD: postgres, POSTGRES_DB: esports_analyzer }
        ports: ["5432:5432"]
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci --prefix backend
      - run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/db/schema.sql && psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f backend/db/migrations/001_governed_coaching.sql
        env: { DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/esports_analyzer" }
      - run: npm test --prefix backend
      - run: npm ci --prefix frontend
      - run: npm run build --prefix frontend
      - run: bash -n start.sh scripts/*.sh
